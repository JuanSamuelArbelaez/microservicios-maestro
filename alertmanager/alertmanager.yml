global:
  smtp_smarthost: 'smtp.gmail.com:465'
  smtp_from: 'mapsphereapp@gmail.com'  # ← Tu email de Gmail
  smtp_auth_username: 'mapsphereapp@gmail.com'  # ← Tu email de Gmail
  smtp_auth_password: 'fyswvztlvpqohlze'  # ← Contraseña de aplicación
  smtp_require_tls: false

route:
  group_by: ['alertname']
  group_wait: 10s
  group_interval: 10s
  repeat_interval: 1h
  receiver: 'team-email'

receivers:
  - name: 'team-email'
    email_configs:
      - to: 'juans.arbelaezr@uqvirtual.edu.co'
        headers:
          subject: '🚨 Alerta: {{ .GroupLabels.alertname }} - Microservicios'
        # CORRECCIÓN: Usar 'text' en lugar de 'body'
        text: |-
          ALERTA DE MICROSERVICIOS
          ========================

          📋 Alerta: {{ .GroupLabels.alertname }}
          🌐 Instancia: {{ .Labels.instance }}
          ⚠️ Severidad: {{ .Labels.severity }}

          📝 Resumen: {{ .Annotations.summary }}
          📄 Descripción: {{ .Annotations.description }}

          🕐 Inicio: {{ .StartsAt }}
          🔚 Fin: {{ .EndsAt }}

          Servicios afectados:
          {{ range .Alerts }}
          - {{ .Labels.instance }}: {{ .Annotations.summary }}
          {{ end }}

          Verificar en Prometheus: http://localhost:9090


